import { CustomLanguageOperationsPipe } from './custom-language-operations.pipe';

describe('CustomLanguageOperationsPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomLanguageOperationsPipe();
    expect(pipe).toBeTruthy();
  });
});
